# =====================================================================
# Multi-Agent Business Assistant — Python Dependencies (requirements.txt)
# Complete library requirements for all 20 Agents, AI models, APIs & Backend
# =====================================================================

# --- 1. Core Environment & Configuration ---
python-dotenv>=1.0.1
pydantic>=2.7.0
pydantic-settings>=2.2.1
requests>=2.31.0
httpx>=0.27.0

# --- 2. AI Models & Multi-Agent Workflow Engine ---
openai>=1.30.0               # Standard SDK architecture used for OpenRouter orchestration
langgraph>=0.1.5             # State & Cyclic multi-agent Supervisor workflow execution
langchain-core>=0.2.0
langchain-community>=0.2.0

# --- 3. Google Workspace Integrations (Email #4 & Calendar #5) ---
google-api-python-client>=2.130.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# --- 4. Web Research & Scraping (Research #2 & Coding #3) ---
tavily-python>=0.4.0         # Primary verified AI internet researcher SDK
duckduckgo-search>=5.3.0     # Free offline fallback search library
beautifulsoup4>=4.12.0       # HTML source document cleaning & extraction
wikipedia>=1.4.0

# --- 5. Vector Store & Local Embeddings (RAG #14 & Memory #8) ---
qdrant-client>=1.9.0         # Zero-server local & cloud vector memory storage
fastembed>=0.2.7             # 100% Free local sentence embeddings (BAAI/bge models)

# --- 6. Report Generation & Office Suite (Report Agent #6) ---
reportlab>=4.2.0             # Automated PDF document creation
python-docx>=1.1.2           # Word (.docx) document builder
openpyxl>=3.1.2              # Excel (.xlsx) spreadsheet analytics & creation
python-pptx>=0.6.23          # PowerPoint (.pptx) slide deck builder

# --- 7. Document Reader & Local File Parser (File #9 & OCR #16) ---
pypdf>=4.2.0                 # Reading & extracting text from PDF attachments
pillow>=10.3.0               # Vision image pre-processing & formatting

# --- 8. Backend API & WebSockets (Phases 3, 4 & 11) ---
fastapi>=0.111.0             # High-speed asynchronous backend API server
uvicorn[standard]>=0.30.0    # ASGI production web & WebSocket server
sqlalchemy>=2.0.30           # Relational SQL database ORM (SQLite & Postgres compatible)
pyjwt>=2.8.0                 # JWT Token creation and authentication validation
pwdlib[argon2]>=0.2.0        # Modern secure password hashing algorithm
